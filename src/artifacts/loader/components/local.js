import React, { useEffect, useState } from "react";

/**
 *
 * @param {"line"|"doughnut"} type
 * @param {} options
 * @return {JSX.Element}
 */
function LocalStrategy({
    type = "line",
    entity,
    options
}) {
    const [chartData, setChartData] = useState(null);

    return (
        <>
        </>
    );
}

export default LocalStrategy;
